<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="100dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:padding="15dp">
    
    <ImageView
        android:id="@+id/image_voiture"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:src="@mipmap/ic_launcher"/>

    <TextView
        android:id="@+id/libelle_voiture"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Voiture nom"
        app:layout_constraintLeft_toLeftOf="@id/image_voiture"
        android:layout_marginLeft="90dp"
        />


    <TextView
        android:id="@+id/debut_date"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Date début"
        app:layout_constraintLeft_toLeftOf="@id/image_voiture"
        app:layout_constraintTop_toBottomOf="@id/libelle_voiture"
        android:layout_marginLeft="90dp"
        />
    <TextView
        android:id="@+id/fin_date"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Date fin"
        app:layout_constraintLeft_toLeftOf="@id/image_voiture"
        app:layout_constraintTop_toBottomOf="@id/debut_date"
        android:layout_marginLeft="90dp"
        />

</android.support.constraint.ConstraintLayout>