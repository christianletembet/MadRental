<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="100dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:padding="15dp">
    
    <ImageView
        android:id="@+id/image"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:src="@mipmap/ic_launcher"/>

    <TextView
        android:id="@+id/libelle_voiture"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Voiture nom - Prix"
        app:layout_constraintLeft_toLeftOf="@id/image"
        android:layout_marginLeft="90dp"
        />

    <TextView
        android:id="@+id/debutDate"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Date début"
        app:layout_constraintLeft_toLeftOf="@id/image"
        app:layout_constraintTop_toBottomOf="@id/libelle_voiture"
        android:layout_marginLeft="90dp"
        />
    <TextView
        android:id="@+id/finDate"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Date fin"
        app:layout_constraintLeft_toLeftOf="@id/image"
        app:layout_constraintTop_toBottomOf="@id/debutDate"
        android:layout_marginLeft="90dp"
        />

</android.support.constraint.ConstraintLayout>